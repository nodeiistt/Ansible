#!/bin/bash
{% if gas_params is defined %}
{% set tx_flags = gas_params %}
{% else %}
{% set tx_flags = '' %}
{% endif %}

# Constants
KEY="{{ nodeist_key }}"
PASSPHRASE="{{ nodeist_secret }}"

echo ${PASSPHRASE} | {{ daemon }} tx bank send $KEY $1 $2  -b sync {{ tx_flags }} --yes
